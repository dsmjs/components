import layoutStyles from '../../layoutStyles.json';

const styles = {
  padding: `9px ${layoutStyles.innerGutterWidth}px 9px 0`,
  color: '#fff',
  textAlign: 'right',
  fontSize: 24
};

export default styles;
